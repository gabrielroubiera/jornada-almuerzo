version: '3.1'
services:
  mysql:
    image: mysql:latest
    container_name: myapp-mysql
    volumes:
      - ./data/mysql:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=rootpass
      - MYSQL_DATABASE=jornadaalmuerzo
      - MYSQL_USER=user
      - MYSQL_PASSWORD=EstDu1601
    ports:
      - "8001:3306"

